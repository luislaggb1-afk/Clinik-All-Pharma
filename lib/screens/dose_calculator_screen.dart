
import 'package:flutter/material.dart';

class DoseCalculatorScreen extends StatelessWidget {
  const DoseCalculatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dose Calculator Screen', style: TextStyle(color: Colors.white)),
    );
  }
}
